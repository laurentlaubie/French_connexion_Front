const initialState = {
  isConnected: false,
};

export default (state = initialState, action = {}) => {
  switch (action.type) {
    // case :
    //   return {
    //     ...state,
    //   };
    default:
      return state;
  }
};
